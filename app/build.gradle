plugins {
    id 'war'
}

dependencies {
    // Tomcat 9 (Java EE / javax.servlet)
    compileOnly 'javax.servlet:javax.servlet-api:4.0.1'

    // PostgreSQL JDBC Driver
    implementation 'org.postgresql:postgresql:42.7.11'
}
